# Optional redirects for a future migration from legacy asset folders with spaces
# to lowercase slug folders. Include only after the new target folders exist.

rewrite ^/assets/Nature\ Seeds/(.*)$ /assets/nature-seeds/$1 permanent;
rewrite ^/assets/Candy\ Shop/(.*)$ /assets/candy-shop/$1 permanent;
rewrite ^/assets/Paint\ Blasters/(.*)$ /assets/paint-blasters/$1 permanent;
rewrite ^/assets/Ball\ is\ God/(.*)$ /assets/ball-is-god/$1 permanent;
