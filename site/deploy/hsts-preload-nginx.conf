# Optional HSTS preload snippet for a fully HTTPS-ready deployment.
# Replace the normal Strict-Transport-Security header with this line only after
# every subdomain is HTTPS-only and the domain owner is ready for browser preload
# behavior to be effectively long-lived.

add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
